#include <stdio.h>
/**
 * main - The main function, prints a statement
 *
 * Return: 0, the function doesn't really return a useful value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
